project_id = "proldgiyterqhthludlp"

[functions.manage-integrations]
verify_jwt = false

[functions.publish-scheduled]
verify_jwt = false

# Autentica por conta própria: JWT do usuário no início/consulta, chave de
# serviço nas chamadas internas de etapa para etapa.
[functions.agencia-pipeline]
verify_jwt = false

# API de automação da agência. Autentica por chave própria (CALU_API_KEY),
# não pelo JWT do gateway — por isso verify_jwt fica desligado.
[functions.calu-api]
verify_jwt = false
