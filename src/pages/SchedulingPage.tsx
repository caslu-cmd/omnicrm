import StubPage from "@/components/StubPage";
import { Calendar } from "lucide-react";
const SchedulingPage = () => <StubPage title="Agendamentos" description="Gerencie sua agenda, crie links de booking e integre com Google Calendar e Outlook." icon={Calendar} />;
export default SchedulingPage;
